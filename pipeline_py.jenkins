pipeline {
   agent any

   stages {
	stage('Install requirements') {
		steps {
	  		sh 'pip install -r requirements.txt'
		}
	}
      stage('Run python tests') {
        steps {      
          sh 'py.test -v -s'
        }
   }

  }
}
